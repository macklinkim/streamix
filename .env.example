# Copy to .env for local dev. Secrets go to Vercel env / Fly secrets in prod (ADR-8, §10).
NODE_ENV=development
PORT=8080
DATABASE_URL=postgres://streamix:streamix@localhost:5432/streamix
REDIS_URL=redis://localhost:6379
